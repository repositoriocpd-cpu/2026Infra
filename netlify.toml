[build]
  command = "npm run build:netlify && node -e \"const fs=require('fs');const p=require('./.build-output.json');console.log(p.buildDir)\" > .publish-dir.txt"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
