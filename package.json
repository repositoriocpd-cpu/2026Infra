{
  "name": "sub-infra-panel",
  "version": "1.0.6",
  "scripts": {
    "build": "npx -y shx rm -rf dist && npx -y shx mkdir -p dist && npx -y shx cp index.html robots.txt 2026_script.js pwa-handler.js manifest.json sw.js _redirects icone_logo.png ui-kit.css ui-kit.js dist/ && npx -y shx cp -r icons dist/ && npx -y shx cp -r public dist/",
    "dev": "npx -y http-server . -p 8080",
    "prebuild": ""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.1"
  }
}
