{
  "name": "sub-infra-panel",
  "version": "1.0.6",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html robots.txt 2026_script.js pwa-handler.js manifest.json sw.js _redirects icone_logo.png dist/ && cp -r icons dist/ && cp -r public dist/",
    "prebuild": ""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.1"
  }
}
