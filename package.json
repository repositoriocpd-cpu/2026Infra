{
  "name": "sub-infra-panel",
  "version": "1.0.6",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.html 2026_script.js pwa-handler.js manifest.json sw.js dist/ && cp -r icons dist/",
    "prebuild": ""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.1"
  }
}
